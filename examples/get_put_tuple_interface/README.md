A simple example meant to demonstrate how to create a module that 
offers ``Get`` and ``Put`` capabilities in its interface.

You can run ``TOPMODULE=mkTop make b_compile`` to confirm that 
``src/Top.bs`` compiles.